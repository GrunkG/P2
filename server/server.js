let mysql = require('mysql');
let fs = require('fs');
let game = require('./blackjack/api');

let sql = {
    host: "localhost",
    user: "username",
    pass: "password",
    db:   "database"
};

//WIP